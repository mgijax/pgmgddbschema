#!/bin/sh

#
# History
#
# 01/27/2022	lec
#	- wts2-767/mgi_notechunk/mgi_note merge
#

cd `dirname $0` && . ./Configuration


cat - <<EOSQL | ${PG_DBUTILS}/bin/doisql.csh $0

CREATE SEQUENCE mrk_biotypemapping_seq;    
ALTER TABLE MRK_BiotypeMapping ALTER _biotypemapping_key SET DEFAULT NEXTVAL('mrk_biotypemapping_seq');
select setval('mrk_biotypemapping_seq', (select max(_biotypemapping_key) from MRK_BiotypeMapping));

EOSQL
