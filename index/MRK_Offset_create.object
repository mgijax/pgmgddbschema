#!/bin/sh

# HISTORY
#    09/16/2008 - jsb - removed MRK_Offset_idx_Marker_key (TR 9037)
#

cd `dirname $0` && . ./Configuration


cat - <<EOSQL | ${PG_DBUTILS}/bin/doisql.csh $0

create index MRK_Offset_idx_cmOffset on mgd.MRK_Offset (cmOffset);


create index MRK_Offset_idx_source on mgd.MRK_Offset (source);



EOSQL
