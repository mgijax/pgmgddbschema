#!/bin/sh

cd `dirname $0` && . ./Configuration

cat - <<EOSQL | ${PG_DBUTILS}/bin/doisql.csh $0

DROP FUNCTION IF EXISTS MGI_resetSequenceNum(varchar,int);

DROP FUNCTION IF EXISTS MGI_resetSequenceNum(text,int,int);

EOSQL
